$schema: "https://moonrepo.dev/schemas/project.json"
id: "riff-cli"
language: "rust"
type: "application"
tags: ["cli"]

tasks:
  publish:
    command: "goreleaser"
    args: ["release", "--clean", "--config", "riff-cli/.goreleaser.yaml"]
    options:
      runInCI: false
